# Gatewright

Read and follow the repository's `AGENTS.md` rules for Gatewright work.

Before the first edit of any task, put the work on the board: `gw brief` to see
what exists, `gw add "<the change>"` for each step you plan to take, then
`gw claim <id>`. The `PreToolUse` guard will refuse edits until you do.
